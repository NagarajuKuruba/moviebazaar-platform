server:
  port: 8083
spring:
  application:
    name: movie-service

  config:
    import: configserver:http://localhost:8888

  datasource:
    url: jdbc:postgresql://localhost:5432/movie_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

  profiles:
    active: dev

